//DMDScript sample script
//Simple read-eval loop

var evalLine = "";
do{
        evalLine = readln();

        try{
                var r = eval(evalLine);
                println(r);
        }catch(e){
                println(e);
        }
}while(evalLine);